<%= form_for(@blank_answer) do |f| %>
  <% if @blank_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blank_answer.errors.count, "error") %> prohibited this blank_answer from being saved:</h2>

      <ul>
      <% @blank_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_field :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
